name: CI
on:
  workflow_call:
  workflow_dispatch:  

jobs:
  commands:
    name: Test
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3   
      - name: Run command
        run: 
          ls -la
